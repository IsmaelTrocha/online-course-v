server:
  port: 8888

spring:
  application:
    name: microservice-config-server
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/configuration-files
          clone-on-start: true
